"""
URL configuration for the payments app.
(Stub — endpoints to be implemented in a future sprint.)
"""
from django.urls import path

app_name = 'payments'

urlpatterns = [
    # TODO: add payments endpoints here
]
