"""
URL configuration for the rides app.
(Stub — endpoints to be implemented in a future sprint.)
"""
from django.urls import path

app_name = 'rides'

urlpatterns = [
    # TODO: add rides endpoints here
]
